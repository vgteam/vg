#!/usr/bin/env bash
#
BASH_TAP_ROOT=../deps/bash-tap
. ../deps/bash-tap/bash-tap-bootstrap

PATH=../bin:$PATH # for vg

plan tests 0

## Test that vectorize correctly vectorizes a small alignment

## Test if vectorize correctly produces formatted output

## Check if vectorize can produce a valid header line.

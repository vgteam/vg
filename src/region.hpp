#ifndef VG_UTILITY_H
#define VG_UTILITY_H

#include <string>

namespace vg {

using namespace std;

void parse_region(
    string& region,
    string& startSeq,
    int& startPos,
    int& stopPos);

}

#endif
